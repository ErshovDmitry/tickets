# Аудит, часть 5 — CLI/UX

Дата: 2026-09-06

- `go test -race ./internal/cli` — успешно.
- Проверены dispatch, help/version, list/show/set/archive/new, ошибки и локализация.

## Замечания
- 🟡 `internal/cli/cmd_list.go`: несколько позиционных аргументов после фильтра молча игнорируются. Опечатка вроде `ticket list active extra` выглядит успешно выполненной. Рекомендация: отклонять более одного positional argument.
- 🟡 Ошибки предупреждений списка печатаются в stderr, но команда всё равно возвращает 0. Это допустимо для tolerant режима, однако следует явно документировать exit-code policy.

**Сделано (2026-09-06).**
